package io.android.movies.features.movies.screen.listeners

internal class MoviesListeners(
    val favoriteListener: FavoriteListener,
    val searchListener: SearchListener,
    val filterListener: FilterListener,
)